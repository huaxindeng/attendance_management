<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ncu.huaxin.attendancemanagement.mapper.ClassMapper">
    <!--    public Employee getEmpById(Integer id);

     public void insertEmp(Employee employee);-->

    <select id="selectById" resultType="Class">
        select * from class where classId=#{classId}
    </select>



<!--    <insert id="insertEmp">-->
<!--        INSERT INTO employee(username,email,gender,d_id) VALUES (#{lastName},#{email},#{gender},#{dId})-->
<!--    </insert>-->
</mapper>